package com.company;
import java.util.Arrays;
import java.util.Scanner;

class Sort {
//сортироавка по левой части
}
if (left < j)
quickSort(array, left, j);
if (right > i)
quickSort(array, i, right);
}
/*
нужно
придумать
свой
массив
*/
System.out.println("Отсортированный массив: " + Arrays.toString(myArray) );

}

}
